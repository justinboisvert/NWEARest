** Please make sure you have C/C++ build tools installed and sqlite3 header files (libsqlite3-dev) before running the next commands will build out native gem extensions

- To set up server dependencies, run the following commnand:

bundle

- To run server, run the following command (with # being your desired port number):

./run-server # 

- To run server tests, run the following command:

ruby test/populate_and_retrieve.rb


